// Empty stub for the `server-only` package under the Vitest Node runner.
// In a real Next.js build `server-only` throws if a server module is pulled into
// a client bundle; in unit tests there is no such boundary, so this is a no-op.
export {}
